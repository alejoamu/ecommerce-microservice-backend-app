# Locust configuration file for E-commerce Microservices Performance Testing

# Host configuration
host = http://192.168.49.2:31396

# User configuration
users = 50
spawn-rate = 5

# Test duration (in seconds)
run-time = 300

# Web UI configuration (where Locust web UI listens, not the target host)
web-host = 0.0.0.0
web-port = 8089

# Logging
logfile = performance-tests/locust.log
loglevel = INFO

# CSV output
csv = performance-tests/results/performance_test

# HTML report
html = performance-tests/results/performance_report.html

# Headless mode (for CI/CD)
headless = false

# Stop timeout
stop-timeout = 30
